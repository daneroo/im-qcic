---
name: SetInterval
route: /setinterval
menu: Components
# fullpage: true
---

import SetInterval from './SetInterval'

# SetInterval

Uses RenderProps

```jsx
<SetInterval delay={1500} render={
  ({stamp})=><div>stamp: {stamp.substr(0,21)}</div>
} />
```

<SetInterval delay={1500} render={
  ({stamp})=><div>stamp: {stamp.substr(0,21)}</div>
} />



## Only once, and immediately

```jsx
<SetInterval once={true}/>
```

<SetInterval once={true}/>


## Default 

delay:1s, iterations:forever, render JSON

```jsx
<SetInterval />
```

<SetInterval />


