---
name: Demo
route: /graphqldemo
menu: GraphQL
# fullpage: true
---

import { GQLDemo } from '../../src/components/GQLDemo'

# Demo

This shows is how you would use `<RegisterApolloProvider />` with a query. Similarly with a subscription.

```jsx
<RegisterApolloProvider httpurl='https://fizzbuzzclock.n.imetrical.com/graphql'>
  <Query query={helloQy} >
    {({ loading, error, data }) => {
      if (loading) return <p>Loading...</p>
      if (error) return <p>Error :(</p>
      return <div>Hello: <b>{data.hello}</b></div>
    }}
  </Query>
</RegisterApolloProvider>
```

## Live

<GQLDemo httpurl="https://fizzbuzzclock.n.imetrical.com/graphql" initialVisible={true} />

<GQLDemo httpurl="https://fizzbuzzclock.n.imetrical.com/graphql" />

<GQLDemo httpurl="http://localhost:7070/graphql" />

## Expressions

These are the grapQL `query` and `subcription` expressions used above:

```js
query {
  hello
}

subscription {
  newMessage {
    id
    stamp
    text
  }
}
```
