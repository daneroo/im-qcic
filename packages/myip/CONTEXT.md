# Packages/myip

An undeployed rewrite of Cloudrun's "myip" service — see `cloudrun/CONTEXT.md`. Per the README TODO, the intent is to "bless, update deps, and publish to gcloud run," retiring `cloudrun/` once this ships. Until then, `cloudrun/` remains the live one.
