---
title: Fetch
desciption: Fetch Component
---

import { Fetch, SetInterval } from '@daneroo/qcic-react'

# Fetch

Uses RenderProps

```jsx
<Fetch url="https://time.qcic.n.imetrical.com/" render={
  ({ loading, error, data })=>{
    if (loading) return <p>Loading...</p>
    if (error) return <p>{error.name}: {error.message}</p>
    return (<pre>Tada:{JSON.stringify(data)}</pre>)
  }
} />
```

<Fetch url="https://time.qcic.n.imetrical.com/" render={
  ({ loading, error, data })=>{
    if (loading) return <p>Loading...</p>
    if (error) return <p>{error.name}: {error.message}</p>
    return (<pre>Tada:{JSON.stringify(data)}</pre>)
  }
} />

## With Error

```jsx
<Fetch url="https://NOTEXIST.time.qcic.n.imetrical.com/" render={
  ({ loading, error, data })=>{
    if (loading) return <p>Loading...</p>
    if (error) return <p>{error.name}: {error.message}</p>
    return (<pre>Tada:{JSON.stringify(data)}</pre>)
  }
} />
```

<Fetch url="https://NOTEXIST.time.qcic.n.imetrical.com/" render={
  ({ loading, error, data })=>{
    if (loading) return <p>Loading...</p>
    if (error) return <p>{error.name}: {error.message}</p>
    return (<pre>Tada:{JSON.stringify(data)}</pre>)
  }
} />
